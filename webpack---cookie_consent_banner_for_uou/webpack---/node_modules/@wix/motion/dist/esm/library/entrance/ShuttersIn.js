import {
    createShuttersPolygon
} from '../../utils';
export default function create(options) {
    const {
        direction = 'right',
            shutters = 12,
            staggered = true
    } = options.namedEffect;
    const easing = options.easing || 'cubicInOut';
    const {
        clipStart,
        clipEnd
    } = createShuttersPolygon(direction, shutters, staggered);
    return [{
        ...options,
        easing,
        keyframes: [{
            clipPath: clipStart
        }, {
            clipPath: clipEnd
        }]
    }];
}
//# sourceMappingURL=ShuttersIn.js.map